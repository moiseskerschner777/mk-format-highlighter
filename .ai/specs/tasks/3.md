# Phase 3 — Bottom bar with live count

**Goal:** A persistent bottom bar appears whenever the current file has ≥1 stacked comment, showing a live count, and hides when the count returns to 0.

**Flow:** Flow 1 — Build→Observe→Adjust

**References:** `design.md` → Component contracts (`MkCommentBarPanel`), Architecture decisions (bottom bar visibility)

---

> **When a task is complete, mark it `[-]`.**

- [-] 3.1 Create `MkCommentBarPanel.kt` as a `JPanel` showing static placeholder text "N comments stacked", not yet wired to real data.

  **Test:**
  - Do: run `./gradlew compileKotlin`
  - Expect: build succeeds

- [-] 3.2 Dock `MkCommentBarPanel` to the bottom of the editor for `.mk` files (via `FileEditor`/`EditorNotifications` integration point chosen during implementation).

  **Test:**
  - Do: run `./gradlew runIde`, open `sample.mk` with zero comments stacked
  - Expect: no bottom bar is visible (starts hidden since count is 0)

- [-] 3.3 Wire the panel's count label to `MkCommentManager.getComments().size`, updating whenever a comment is added.

  **Test:**
  - Do: use "Add Comment" (Phase 2) to stack one comment
  - Expect: bottom bar appears showing "1 comment stacked"

- [-] 3.4 Confirm the count updates live for multiple comments and correct singular/plural text.

  **Test:**
  - Do: stack a second comment on a different line
  - Expect: bar text updates to "2 comments stacked" without needing to switch files or reopen the editor
