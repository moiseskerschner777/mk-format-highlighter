# Phase 5 — Persistence

**Goal:** Stacked comments survive closing and reopening the file, saved to a sidecar JSON file next to the source.

**Flow:** Flow 1 — Build→Observe→Adjust

**References:** `design.md` → Component contracts (`MkCommentStorage`), File structure

---

> **When a task is complete, mark it `[-]`.**

- [-] 5.1 Create `MkCommentStorage.kt` with `save(file, comments)`, serializing to `.mk-comments/<filename>.json` next to the source file (creating the `.mk-comments/` directory if needed).

  **Test:**
  - Do: run `./gradlew compileKotlin`
  - Expect: build succeeds

- [-] 5.2 Wire `save()` to run whenever a comment is added or removed (simplest correct trigger — refine later if too chatty).

  **Test:**
  - Do: run `./gradlew runIde`, stack one comment on `sample.mk`, then check the sandbox project's file system next to `sample.mk`
  - Expect: `.mk-comments/sample.mk.json` exists and contains the comment's line, snippet, and request text

- [-] 5.3 Add `MkCommentStorage.load(file)`, deserializing the JSON back into a list of comments with their original offsets.

  **Test:**
  - Do: run `./gradlew compileKotlin`
  - Expect: build succeeds

- [-] 5.4 Wire `load()` to run on file open, re-creating `RangeMarker`s in `MkCommentManager` from the loaded offsets.

  **Test:**
  - Do: with the comment from 5.2 saved, close `sample.mk` in the sandbox editor, then reopen it
  - Expect: gutter icon reappears on the correct line and the bottom bar shows "1 comment stacked" without re-adding it manually

- [-] 5.5 Confirm a full IDE sandbox restart also reloads comments (not just a file close/reopen within the same session).

  **Test:**
  - Do: stop `./gradlew runIde`, run it again, open `sample.mk`
  - Expect: previously stacked comment is still shown (gutter icon + bar count)