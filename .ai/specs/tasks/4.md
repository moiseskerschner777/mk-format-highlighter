# Phase 4 — Copy-to-clipboard formatting

**Goal:** Clicking "Copy for agent" puts a correctly formatted, paste-ready text block of all stacked comments onto the system clipboard.

**Flow:** Flow 1 — Build→Observe→Adjust

**References:** `design.md` → Component contracts (`MkCommentFormatter`), Data flow

---

> **When a task is complete, mark it `[-]`.**

- [ ] 4.1 Add a "Copy for agent" button to `MkCommentBarPanel`, visible alongside the count.

  **Test:**
  - Do: run `./gradlew runIde`, stack one comment
  - Expect: "Copy for agent" button appears in the bottom bar next to the count

- [ ] 4.2 Create `MkCommentFormatter.kt` with `format(fileName, comments): String`, producing the block format from `design.md` (file name header, then `line N: "snippet"` followed by `> request` per comment).

  **Test:**
  - Do: run `./gradlew compileKotlin`
  - Expect: build succeeds

- [ ] 4.3 Wire the "Copy for agent" button to call `MkCommentFormatter.format()` with the current file's comments and set the result on the system clipboard via `CopyPasteManager`.

  **Test:**
  - Do: with 2 comments stacked (line 2 and line 31 of `sample.mk` per Phase 2), click "Copy for agent", then paste into a plain text editor
  - Expect: pasted text shows the file name header and both comments formatted as `line 2: "..."` / `> ...` and `line 31: "..."` / `> ...`

- [ ] 4.4 Confirm "Copy for agent" does not clear the stack.

  **Test:**
  - Do: after 4.3, look at the bottom bar
  - Expect: count still reads "2 comments stacked" — copying is non-destructive
