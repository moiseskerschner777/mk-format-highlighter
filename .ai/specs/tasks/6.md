# Phase 6 — Clear/resolve

**Goal:** Individual comments can be cleared from the gutter popup, and "Clear all" empties the whole stack from the bottom bar — both update the gutter, bar count, and sidecar JSON immediately.

**Flow:** Flow 1 — Build→Observe→Adjust

**References:** `design.md` → Component contracts (`MkCommentGutterProvider`, `MkCommentManager`), Behavior table

---

> **When a task is complete, mark it `[-]`.**

- [-] 6.1 Make the gutter icon clickable, opening a small popup listing the comment(s) on that line with their request text.

  **Test:**
  - Do: run `./gradlew runIde`, stack a comment on line 2 of `sample.mk`, click its gutter icon
  - Expect: popup appears showing the request text typed earlier

- [-] 6.2 Add a clear "x" per comment inside the popup, calling `MkCommentManager.removeComment(id)` on click.

  **Test:**
  - Do: click the "x" next to the comment shown in 6.1
  - Expect: popup closes, gutter icon on line 2 disappears, bottom bar count decrements by 1

- [-] 6.3 Add a "Clear all" button to `MkCommentBarPanel`, calling `MkCommentManager.clearAll()`.

  **Test:**
  - Do: with 2 comments stacked, click "Clear all" in the bottom bar
  - Expect: both gutter icons disappear, bottom bar itself disappears (count returns to 0)

- [-] 6.4 Confirm clearing updates the sidecar JSON immediately, not just in-memory state.

  **Test:**
  - Do: after 6.3, check `.mk-comments/sample.mk.json` in the sandbox project's file system
  - Expect: file is empty (`[]`) or removed entirely

- [-] 6.5 Confirm a cleared comment does not reappear after closing and reopening the file.

  **Test:**
  - Do: close `sample.mk`, reopen it
  - Expect: no gutter icons, no bottom bar — matches the empty state from 6.4