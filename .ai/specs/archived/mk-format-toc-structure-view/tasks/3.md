# Phase 3 — Real hierarchy

**Goal:** Structure panel shows `:` lines nested by depth; collapsing a parent hides its children.

**Flow:** Flow 1 — Build→Observe→Adjust

**References:** `design.md` → Hierarchy algorithm, Behavior table

---

> **When a task is complete, mark it `[-]`.**

- [-] 3.1 Implement the stack-based hierarchy builder in `MkStructureViewTreeModel.buildRoot()`. Replace the flat list with the depth-stack algorithm from `design.md`: maintain a `stack: Array<MkStructureViewElement?>(7)`, attach each node to `stack[depth - 1]` as a child, clear deeper stack slots on each insertion, add depth-0 nodes to `rootChildren`.

  **Test:**
  - Do: `./gradlew runIde`, open a `.mk` file with at least two levels of `:` nesting (e.g. a depth-0 parent containing depth-1 children), press Alt+7.
  - Expect: depth-1 entries appear indented under their depth-0 parent in the panel. Collapsing the parent hides its children.

- [-] 3.2 Commit changes from 3.1.

  **Test:**
  - Run: `git add -A && git commit -m "3.1: depth-based hierarchy in Structure panel"`
  - Expect: commit succeeds and `git status` shows a clean working tree.

- [-] 3.3 Verify skipped-depth handling — open a `.mk` file where a depth-0 `:` line is directly followed by a depth-2 `:` line (no depth-1 in between).

  **Test:**
  - Do: `./gradlew runIde`, open the skipped-depth file, press Alt+7.
  - Expect: the depth-2 node appears as a child of the depth-0 node with no phantom intermediate node. No exception in the IDE log.

- [-] 3.4 Commit changes from 3.3 (fix only if behavior was wrong; commit either way).

  **Test:**
  - Run: `git add -A && git commit -m "3.3: verify skipped-depth hierarchy"`
  - Expect: commit succeeds and `git status` shows a clean working tree.

- [-] 3.5 Verify auto-scroll from source — move the caret inside a nested section in the editor.

  **Test:**
  - Do: `./gradlew runIde`, open a `.mk` file with nested `:` sections, press Alt+7, move the caret into a nested section by clicking in the editor.
  - Expect: the matching TOC entry highlights automatically without clicking in the Structure panel.

- [-] 3.6 Commit changes from 3.5 (fix only if behavior was wrong; commit either way).

  **Test:**
  - Run: `git add -A && git commit -m "3.5: verify auto-scroll from source"`
  - Expect: commit succeeds and `git status` shows a clean working tree.